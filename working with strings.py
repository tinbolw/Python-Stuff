phrase = "string"
print(phrase.replace("ri", "ra")) # \n to insert new line
# \ is escape character, use it to type literals
# like \"
# + "" adds to string
# use functions with a period after the variable
# you can use multiple functions with one variable, but you still have to end the function with brackets.
# len () to check length
# some functions are at the beginning of the variable instead of the end.
# you can use square brackets to get letters (starts with [0])
# .index check position of characters "parameters"
# .replace to replace stuff .replace("x", "y")
# or straight variables
print("done")
